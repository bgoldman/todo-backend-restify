var os = require('hostname');

module.exports = {
    "server": {
        "api_root":    "http://todo-backend-restify.heroku.com",
        "environment": "production",
        "port":        process.env.PORT
    }
};
